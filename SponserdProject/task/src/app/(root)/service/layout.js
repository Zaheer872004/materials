import React from "react";

export const metadata = {
  title: "Bhaw Bhaw | Service",
  description: "Discover a variety of pet care services tailored to meet your needs, from grooming to health checkups.",
};

const ServiceLayout = ({ children }) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default ServiceLayout;
